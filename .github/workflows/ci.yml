name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Assert no .env is tracked
        run: |
          git ls-files --error-unmatch .env 2>/dev/null && { echo "::error::.env is tracked"; exit 1; }
          echo "ok: .env is not tracked"

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install
        run: |
          pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements.txt

      - name: Lint
        run: ruff check .

      - name: Test
        run: pytest -v

      # Audits the declared requirements rather than the environment: the
      # installed torch is a `+cpu` local build that is absent from PyPI, and
      # --strict fails on anything it cannot look up.
      - name: Dependency audit
        run: pip-audit -r requirements.txt --strict --desc

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci --no-audit --no-fund
      - run: npm run build
